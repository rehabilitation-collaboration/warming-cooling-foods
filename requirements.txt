# Pinned to the interpreter and package versions the reported results were
# produced on (Python 3.14.3). These are the versions named in the manuscript's
# Data Availability statement; the earlier pins here did not match the
# environment the analysis actually ran in.
beautifulsoup4==4.15.0
lxml==6.1.1
markdown==3.10.2
matplotlib==3.10.8
numpy==2.4.2
pandas==3.0.1
pytest==9.1.1
requests==2.34.2
scipy==1.17.1
statsmodels==0.14.6
weasyprint==68.1
